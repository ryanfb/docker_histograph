api: 
  admin: 
    name: histograph
    password: histograph
  dataDir: "/tmp/histograph-api-data"
  baseUrl: "http://192.168.99.100:3001"

import:
  dirs:
    - ../data

neo4j:
  host: neo4j

redis:
  host: redis

elasticsearch:
  host: elasticsearch

data:
  bag:
    db:
      host: postgres

core:
  batchTimeout: 100
  batchSize: 250
